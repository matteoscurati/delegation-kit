{
  "title": "Builder qualification attempt output",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "answers"],
  "properties": {
    "status": {"type": "string", "enum": ["completed"]},
    "answers": {
      "type": "object",
      "additionalProperties": false,
      "required": ["changed_paths", "summary"],
      "properties": {
        "changed_paths": {"type": "array", "items": {"type": "string"}},
        "summary": {"type": "string"}
      }
    }
  }
}
